<%= form_with(model: measurement, local: true) do |form| %>
  <% if measurement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(measurement.errors.count, "error") %> prohibited this measurement from being saved:</h2>

      <ul>
      <% measurement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :measurement_user_id %>
  </div>

  <div class="field">
    <%= form.label :bmi_range_id %>
    <%= form.text_field :bmi_range_id, id: :measurement_bmi_range_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
